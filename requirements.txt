# PDF Extraction Pipeline Dependencies

# PDF text extraction libraries (install at least one)
# PyMuPDF - Best quality extraction, handles most PDFs well
pymupdf>=1.23.0

# pdfplumber - Good for PDFs with tables and structured content
pdfplumber>=0.10.0

# PyPDF2 - Fallback option, works for simple PDFs
pypdf2>=3.0.0

# Optional: For better OCR support on scanned PDFs
# pytesseract>=0.3.10
# pillow>=10.0.0

# Optional: For downloading PDFs from cloud storage
# google-api-python-client>=2.0.0
# google-auth-httplib2>=0.1.0
# google-auth-oauthlib>=1.0.0
